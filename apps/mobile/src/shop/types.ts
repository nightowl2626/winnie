export type ShopGeoPermission = "unknown" | "granted" | "denied" | "error";

export type ShopCoords = {
  lat: number;
  lng: number;
};

export type ShopMapSize = {
  width: number;
  height: number;
};
